extends layout

include utils

block content
    nav(class='nav-bar')
        a(href='/') Home
        a(href='/questions') Questions
        form(class='search-bar' action='/questions/search' method='post')
            +csurf(csrfToken)
            input(type='search' placeholder='Search Questions Dummy' name='search' id='search')
        a(href='/users/logout') Log Out
    block name
